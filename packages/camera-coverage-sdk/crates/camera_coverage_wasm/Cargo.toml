[package]
name = "camera_coverage_wasm"
version = "0.1.0"
edition = "2021"
description = "BVH build, voxelization, flood-fill, and SVO construction for the camera-coverage engine (§4, §6.2, §10, §9.5)"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
